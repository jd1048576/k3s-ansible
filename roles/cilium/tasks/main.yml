- name: Helm info cilium
  kubernetes.core.helm_info:
    release_name: cilium
    release_namespace: kube-system
  delegate_to: localhost
  register: cilium_helm_info

- name: Helm install cilium-preflight
  when: cilium_helm_info.status is defined and not(cilium_helm_info.status.chart is match("cilium-" + cilium_version))
  block:
    - name: Helm install cilium pre-flight
      kubernetes.core.helm:
        chart_ref: oci://quay.io/cilium/charts/cilium
        chart_version: "{{ cilium_version }}"
        release_name: cilium-preflight
        release_namespace: kube-system
        release_values: "{{ cilium_preflight_values }}"
      delegate_to: localhost

    - name: Check cilium pre-flight DaemonSet
      kubernetes.core.k8s_info:
        api_version: apps/v1
        kind: DaemonSet
        name: cilium-pre-flight-check
        namespace: kube-system
        wait: true
        wait_timeout: 600
      delegate_to: localhost

    - name: Check cilium pre-flight Deployment
      kubernetes.core.k8s_info:
        api_version: apps/v1
        kind: Deployment
        name: cilium-pre-flight-check
        namespace: kube-system
        wait: true
      delegate_to: localhost

    - name: Helm uninstall cilium pre-flight
      kubernetes.core.helm:
        release_name: cilium-preflight
        release_namespace: kube-system
        release_state: absent
      delegate_to: localhost

- name: Helm install cilium
  kubernetes.core.helm:
    chart_ref: oci://quay.io/cilium/charts/cilium
    chart_version: "{{ cilium_version }}"
    release_name: cilium
    release_namespace: kube-system
    release_values: "{{ cilium_values }}"
  delegate_to: localhost

- name: Check cilium operator
  kubernetes.core.k8s_info:
    api_version: apps/v1
    kind: Deployment
    name: cilium-operator
    namespace: kube-system
    wait: true
  delegate_to: localhost

- name: Check cilium agent
  kubernetes.core.k8s_info:
    api_version: apps/v1
    kind: DaemonSet
    name: cilium
    namespace: kube-system
    wait: true
    wait_timeout: 600
  delegate_to: localhost

- name: Check cilium envoy
  kubernetes.core.k8s_info:
    api_version: apps/v1
    kind: DaemonSet
    name: cilium-envoy
    namespace: kube-system
    wait: true
    wait_timeout: 300
  delegate_to: localhost

- name: Create internal cilium cidr group
  kubernetes.core.k8s:
    state: present
    resource_definition:
      apiVersion: cilium.io/v2
      kind: CiliumCIDRGroup
      metadata:
        name: internal
      spec:
        externalCIDRs:
          - "{{ (ansible_facts['default_ipv4']['address'] + '/' + ansible_facts['default_ipv4']['netmask']) | ansible.utils.ipaddr('network/prefix') }}"
  delegate_to: localhost

- name: Fetch Cloudflare IPv4 list
  ansible.builtin.uri:
    url: https://www.cloudflare.com/ips-v4
    return_content: true
  delegate_to: localhost
  register: cilium_cloudflare_ips

- name: Create cloudflare cilium cidr group
  kubernetes.core.k8s:
    state: present
    resource_definition:
      apiVersion: cilium.io/v2
      kind: CiliumCIDRGroup
      metadata:
        name: cloudflare
      spec:
        externalCIDRs: "{{ cilium_cloudflare_ips.content | ansible.builtin.split | ansible.builtin.sort }}"
  delegate_to: localhost

- name: Create host cilium network policy
  kubernetes.core.k8s:
    state: present
    resource_definition:
      apiVersion: cilium.io/v2
      kind: CiliumClusterwideNetworkPolicy
      metadata:
        name: host
      spec:
        nodeSelector: {}
        ingress:
          - fromEntities:
              - cluster
          - fromCIDRSet:
              - cidrGroupRef: internal
          - icmps:
              - fields:
                  - type: EchoRequest
                    family: IPv4
        egress:
          - toEntities:
              - all
  delegate_to: localhost

- name: Create cilium load balancer ip pool
  kubernetes.core.k8s:
    state: present
    resource_definition:
      apiVersion: cilium.io/v2
      kind: CiliumLoadBalancerIPPool
      metadata:
        name: default
      spec:
        blocks:
          - start: "{{ ansible_facts['default_ipv4']['address'] }}"
  delegate_to: localhost

- name: Create cilium l2 announcement policy
  kubernetes.core.k8s:
    state: present
    resource_definition:
      apiVersion: cilium.io/v2alpha1
      kind: CiliumL2AnnouncementPolicy
      metadata:
        name: default
      spec:
        interfaces:
          - ^eth[0-9]+
        loadBalancerIPs: true
  delegate_to: localhost
